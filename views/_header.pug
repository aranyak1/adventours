header.header
    nav.nav.nav--tours
        a.nav__el.All-tours(href='/') All tours
    h1.heading-1 Adventours
    nav.nav.nav--user
    if user
        a.nav__el.nav__el--cta.nav__el--logout log out
        a.nav__el.user-img(href='/me')
            img.nav__user-img(src=`img/users/${user.photo}` alt=``)
            span.nav__el--ctb= user.name.split(' ')[0]
    else
        a.nav__el--cta.nav__el(href='/login') Log in
        a.nav__el.nav__el--cta(href='/signUp') Sign up